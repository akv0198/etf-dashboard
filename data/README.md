# Data pipeline workspace

Runtime data is written to `public/data/dashboard.json` so Vite/GitHub Pages can serve it as a static asset. This folder is reserved for optional raw provider downloads or local caches if you extend the pipeline.
